<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:top="0dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <padding android:bottom="4dp" android:left="4dp" android:top="4dp" android:right="4dp"/>
            <solid android:color="@color/light_pink" /> <!-- Set the same background color as above -->
            <corners android:topLeftRadius="26dp"
                android:topRightRadius="26dp"
                android:bottomLeftRadius="26dp"
                android:bottomRightRadius="26dp"/> <!-- Adjust the radius as needed -->
            <stroke android:color="@color/purple" android:width="1dp"/>
        </shape>
    </item>
</layer-list>